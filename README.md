# GPUI Component Extras

This repository is intended to host additional GPUI components built on top of `gpui` and
`gpui-component`.

## Run

- Story app: `cargo run`
- Rich text example: `cargo run --example richtext`
- Plate toolbar buttons: `cargo run --example plate_toolbar_buttons`
